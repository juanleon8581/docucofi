---
trigger: always_on
---

- Use of PNPM as a package manager
- Your code must match the project Prettier configuration
- Use TypeScript and avoid using the ‘any’ type.
- The memory of activeContext.md must be kept fed and updated with the relevant context of the tasks being performed by the agent.
